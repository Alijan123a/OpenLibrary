# Optional: override defaults for docker-compose
# Copy to .env and adjust as needed

# Auth service secrets (change in production)
DJANGO_SECRET_KEY=dev-secret-change-in-production
SIMPLEJWT_SECRET=dev-jwt-secret
AUTH_INTERNAL_KEY=openlibrary-internal-key
