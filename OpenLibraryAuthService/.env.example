# Django secret key (change in production)
DJANGO_SECRET_KEY=your-secret-key-here

# JWT signing secret (shared with services that verify tokens; defaults to DJANGO_SECRET_KEY if omitted)
SIMPLEJWT_SECRET=your-jwt-secret-here
